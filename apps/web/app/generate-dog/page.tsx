export default function GenerateDogPage() {
  return (
    <main style={{ padding: "40px", fontFamily: "sans-serif" }}>
      <h1>Generate Dog (Test)</h1>

      <button>Generate Dog</button>

      <p>Dog output will appear here.</p>
    </main>
  );
}