# ShowRing Product Documentation

[`master-file.md`](master-file.md) is the current ShowRing design authority: it defines what the game means and the design rules it preserves.

Historical Master Files and design notes remain elsewhere in `docs/` for reference. They are not current authority unless the Master File explicitly incorporates them. Implementation location, current code paths, and architecture boundaries belong in [Architecture Documentation](../ARCHITECTURE/README.md).
